{
    "name": "webscraper",
    "version": "1.0.0",
    "description": "Aggressive web scraper - paste any URL, scrape everything",
    "scripts": {
        "dev": "uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload",
        "prod": "uvicorn api.main:app --host 0.0.0.0 --port 8000 --workers 4",
        "test": "pytest tests/ -v",
        "lint": "ruff check .",
        "format": "ruff format ."
    }
}
